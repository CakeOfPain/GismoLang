
Person(txt name, int age) struct;

actor main(txt[] args) {
	// 1. &3 as leo
	// 2. "Leo"
	// 3. Person(&4, 20)
	// 4. "Hello World"
	leo := Person("Leo", 20)
	leo.birthday()
}

birthday(Person person) {
	// 4. &3 as person (import)
	person.age += 1
	person.name += "Hello World"
}

// {STACKFRAME 1} <-| {STACKFRAME 2} <-| {STACKFRAME 3} ...
// I) Stackframes mit geringerer Lebenszeit beeinflussen mit mehr
