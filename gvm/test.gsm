
String(int count, txt value) table;

vanguard main() {
	username := String(3, "Leo")
	output(username)
	output(username.count)
	output(username.value)
	output(username.classOf())

	// for i = 0; i < ObjectNode_name@classic.Size(); i+=1 {
	// 	output("Object: ", ObjectNode_name@classic[i])
	// }
}
